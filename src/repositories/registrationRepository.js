export class RegistrationRepository {
  register(eventID, userID) { return null; }
  listByEvent(eventID) { return []; }
  countAttendeesForEvent(eventID) { return 0; }
  countAllAttendees() { return 0; }
}
